@model IEnumerable<DemoSo1.Models.HoaDon>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HangHoa.TenHang)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TenHoaDon)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NgayMua)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HangHoa.TenHang)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TenHoaDon)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NgayMua)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_HD }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_HD }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_HD })
        </td>
    </tr>
}

</table>
